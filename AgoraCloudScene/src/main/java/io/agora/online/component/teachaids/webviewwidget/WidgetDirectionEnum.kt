package io.agora.online.component.teachaids.webviewwidget

enum class WidgetDirectionEnum (val value: Int) {
    LT(0),//左上 left top
    RT(1),//右上 right top
    LB(2),//左下
    RB(3),//右下
    MOVE(4)// 移动
}
